<section class="section">
    <div class="container ">
        <div class="content-section">
            <div class="row justify-content-center align-items-center admin-menu">
                <a href="{{  route('userOverview') }}" class="mx-3">Users</a>
                <a href="{{  route('adminOverview') }}" class="mx-3">Administrators</a>
                <a href="{{  route('postOverview') }}" class="mx-3">Posts</a>
                <a href="{{  route('categoryOverview') }}" class="mx-3">Categories</a>
            </div>
        </div>
    </div>
</section>
